import NavBar from "../../routes/NavBar";

export default function Dashboard() {
    

    return (
        <div>
            <NavBar />
            <h1>Dashboard page</h1>
        </div>

    );
};